<%= form_for :user, url: '/sessions', method: :post do |f| %>
  <label for="phone">Phone Number</label>
  <%= f.text_field :phone %>
  <%= f.submit "Login", class: "", id: 'login-button' %>
<% end %>
